import 'package:flutter/material.dart';

// This one should be able for editing and addding
class PostAddingFullScreenDialog extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
