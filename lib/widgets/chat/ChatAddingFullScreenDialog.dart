import 'package:flutter/material.dart';

class ChatAddingFullScreenDialog extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("聊天室創建"),
      ),
    );
  }
}
